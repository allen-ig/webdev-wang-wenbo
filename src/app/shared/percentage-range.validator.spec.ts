import { PercentageRangeValidator } from './percentage-range.directive';

describe('PercentageRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new PercentageRangeValidator();
    expect(directive).toBeTruthy();
  });
});
