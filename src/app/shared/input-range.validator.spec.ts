import { HeaderSizeValidator } from './header-size.directive';

describe('HeaderSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderSizeValidator();
    expect(directive).toBeTruthy();
  });
});
