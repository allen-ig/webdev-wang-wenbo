import { FileInputValidator } from './file-input.directive';

describe('FileInputDirective', () => {
  it('should create an instance', () => {
    const directive = new FileInputValidator();
    expect(directive).toBeTruthy();
  });
});
