import { ConfirmEqualValidator } from './confirm-equal.validator';

describe('ConfirmEqualValidator', () => {
  it('should create an instance', () => {
    const directive = new ConfirmEqualValidator();
    expect(directive).toBeTruthy();
  });
});
